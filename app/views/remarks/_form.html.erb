<%= form_with(model: remark, local: true) do |form| %>
  <% if remark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(remark.errors.count, "error") %> prohibited this remark from being saved:</h2>

      <ul>
      <% remark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :remark %>
    <%= form.text_area :remark, id: :remark_remark %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date, id: :remark_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
