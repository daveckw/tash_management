<%= form_with(model: project_task, local: true) do |form| %>
  <% if project_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_task.errors.count, "error") %> prohibited this project_task from being saved:</h2>

      <ul>
      <% project_task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, id: :project_task_name %>
  </div>

  <div class="form-group">
    <%= form.label :status %>
    <%= form.select :status, options_for_select(ProjectTask.statuses.keys.to_a), {id: :project_task_status, class: "form-control"} %>
  </div>

  <div class="form-group">
    <%= form.label :dateline %>
    <%= form.date_select :dateline, id: :project_task_dateline %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
